@{
    ViewData["Title"] = "Присоединение к событию";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card">
                <div class="card-body p-5">
                    <div class="text-center mb-4">
                        <div class="feature-icon bg-success bg-opacity-10 mb-3">
                            <i class="bi bi-people text-success" style="font-size: 2rem;"></i>
                        </div>
                        <h2 class="fw-bold">Присоединиться к событию</h2>
                        <p class="text-muted">Введите код события и ваш никнейм</p>
                    </div>

                    <form method="post">
                        <div class="mb-4">
                            <label class="form-label fw-semibold">Код события</label>
                            <div class="input-group">
                                <span class="input-group-text">
                                    <i class="bi bi-key"></i>
                                </span>
                                <input type="text" name="code" class="form-control" placeholder="XXXX-XXXX-XXXX" required>
                            </div>
                            <div class="form-text">
                                Код события должен быть предоставлен организатором
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-semibold">Ваш никнейм</label>
                            <div class="input-group">
                                <span class="input-group-text">
                                    <i class="bi bi-person-badge"></i>
                                </span>
                                <input type="text" name="nickname" class="form-control" placeholder="Как вас будут видеть участники" required>
                            </div>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-success btn-lg">
                                <i class="bi bi-arrow-right-circle me-2"></i>Присоединиться
                            </button>
                            <a href="/Home/Menu" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left me-2"></i>Назад в меню
                            </a>
                        </div>
                    </form>

                    <div class="mt-4 pt-3 border-top">
                        <div class="alert alert-light">
                            <h6 class="alert-heading">
                                <i class="bi bi-question-circle me-2"></i>Не знаете код события?
                            </h6>
                            <p class="mb-0 small">Обратитесь к организатору встречи, чтобы получить код.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>